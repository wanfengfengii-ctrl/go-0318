# 深海采样器压力舱耐压验证与航次准入闭环

## 项目目标

建设一个可编译运行并包含真实操作页面的 Go Web 服务，覆盖深海采样器压力舱从试验构型冻结、传感器装配、分级加压、恒压保压、泄压检查到航次准入签发的完整流程。核心采用只追加试验事件、整数压力计算、阶段状态机、互斥设备租约和单写终态屏障。SQLite 持久化构型、试验轮次、样点、设备调用、异常传播、复核及准入凭据；进程重启后依据已提交事件恢复物化状态。生产 Go 代码规划为20至28个文件、至少5个业务包、约2500有效行，严格控制在2000至3000行，测试、前端、注释和重复填充不计入规模。前端提供构型冻结、加压控制、实时证据表、异常复验、复核和准入结果页面，全部操作连接真实HTTP接口。项目提供可复现前端构建及适用于amd64、arm64的Docker构建方案。

## 端到端业务流程

1. 构型冻结：试验负责人提交舱体、端盖、密封圈、紧固件、压力传感器、温度传感器、泵组、阀组、目标深度、压力阶梯、升降压速率、恒压时长、泄漏阈值和校准摘要；服务规范化排序并生成不可变摘要。
2. 原子开试：将部件序列号绑定至唯一安装位，同时申请试验舱位、泵组、采集器和阀组租约；绑定与全部租约在同一事务内成功或整体回滚。
3. 分级耐压：操作者依次执行预检、充液排气、分级加压、恒压保压、受控泄压和复压确认。服务按逻辑时钟接收压力、温度、流量、阀位及设备回执，拒绝乱序样点、越级操作和过期租约。
4. 证据判定：使用整数算法计算温度补偿压力、升压速率、压力回落、估算泄漏率和阀门响应延迟；只有连续窗口满足稳定性、泄漏率和超压限制时，当前压力阶梯才可完成。
5. 故障与复验：设备超时、校准过期、响应矛盾只形成确定性重试记录。超压、异常压降、密封渗漏或阀位失配触发隔离，并按舱段、密封边界和共享管路生成排序去重的复验范围；复装后创建新轮次，旧证据保持只读。
6. 准入签发：泄压后完成外观检查、密封复查和两名不同资质人员独立复核。终态屏障在准入、待复验和终止之间仅允许一个结果；满足全部条件时生成唯一航次准入凭据，重启后仍可验证。

## 核心组件与职责

1. 试验构型目录：冻结舱体结构、安装位、压力阶梯、校准摘要和整数判定参数，校验密封边界与管路连通关系。
2. 耐压试验聚合：维护试验轮次、阶段、阶梯进度、只追加事件、聚合版本及可重建物化状态。
3. 部件绑定与资源租约：保证序列号和安装位唯一有效绑定，按规范顺序原子申请、续期和判定设备租约。
4. 采样与证据引擎：验证样点顺序和设备回执，计算补偿压力、速率、回落、泄漏率、响应延迟及稳定窗口。
5. 异常复验与准入仲裁：传播复验范围、隔离旧轮次、校验独立复核，并通过终态屏障签发唯一凭据。
6. HTTP服务与操作前端：提供版本化JSON接口、稳定错误协议和连接后端的构型、试验、证据、复验及准入页面。

## 领域规则与不变量

1. 构型必须包含一个压力入口、至少一个有效压力传感器、闭合的承压舱段及覆盖每条密封边界的检查定义；重复安装位、孤立舱段、未校准通道或不连通管路均拒绝冻结。
2. 压力使用帕、温度使用毫摄氏度、流量使用微升每秒、时间使用毫秒，均存为int64。温度补偿压力、速率和泄漏率采用冻结公式及向零截断；每次乘加、取绝对值和除法均检查溢出与除零。
3. 阶段固定为预检、充液排气、分级加压、恒压保压、受控泄压、复压确认、外观检查、密封复查、独立复核、准入。阶段和压力阶梯只能形成连续前缀。
4. 样点逻辑时刻必须严格递增，并归属冻结的半开采样窗口。温度补偿后的压力不得超过舱体上限；升压速率、压力回落和泄漏率必须在当前阶梯阈值内。
5. 同一部件序列号和安装位在任一时刻只能存在一个有效绑定。多资源申请先按资源类型和编号排序，再于单事务内检查及写入；提交证据时重新验证租约所有者、轮次和有效期。
6. 所有写请求携带操作号和规范化请求摘要；相同内容返回原结果，不同内容返回IDEMPOTENCY_CONFLICT。设备失败不得生成合格证据，只能生成带固定重试序号和下一逻辑时刻的调用记录。
7. 异常复验范围由异常位置沿密封边界、所属舱段和共享管路确定传播，结果按舱段、安装位和检查类型排序去重。新轮次不得接纳旧轮次的迟到样点或设备回执。
8. 准入要求当前轮次构型一致、全部阶梯完成、证据窗口无缺口、泄压归零、检查通过、复验范围清空且两名不同人员在复核时资质有效；凭据由构型摘要、轮次和证据根摘要确定生成。

## 数据模型与持久化

1. configuration_snapshots、chambers、seal_boundaries、ports与pressure_steps：保存不可变构型、连通关系、安装位、阶梯参数及规范摘要。
2. trials、rounds、trial_events与aggregate_versions：保存当前轮次、阶段、终态、事件序号和乐观并发版本。
3. components、install_positions与bindings：保存部件序列号、类型、安装位及轮次化有效绑定，并以数据库唯一约束阻止重复占用。
4. resources与leases：保存舱位、泵组、采集器、阀组、持有者、租约令牌和逻辑到期时刻。
5. samples、device_calls、valve_receipts与evidence_windows：只追加保存原始整数样点、重试调用、阀位回执及计算证据。
6. retest_sets与retest_members：保存异常类型、传播依据、受影响舱段、安装位和确定排序。
7. reviews、terminal_barriers与admission_credentials：保存资质快照、独立复核、终态竞争及唯一凭据。
8. idempotency_records与schema_migrations：保存操作摘要、响应、事务结果和数据库迁移版本。

## 公开接口

1. POST /api/v1/configurations/freeze 冻结构型；GET /api/v1/configurations/{digest} 查询规范化快照。
2. POST /api/v1/trials 创建试验；GET /api/v1/trials/{id} 查询阶段、轮次、租约、异常和终态。
3. POST /api/v1/trials/{id}/startup 原子提交部件绑定与资源集合；POST /api/v1/trials/{id}/leases/renew 续期租约。
4. POST /api/v1/trials/{id}/samples 与 /device-results 接收样点及回执；POST /api/v1/trials/{id}/steps/{step}/complete 完成压力阶梯；POST /api/v1/trials/{id}/stages/{stage} 推进阶段。
5. POST /api/v1/trials/{id}/anomalies 生成复验范围；POST /api/v1/trials/{id}/restart 创建新轮次；GET /api/v1/trials/{id}/evidence 查询规范排序的证据链。
6. POST /api/v1/trials/{id}/reviews 提交复核；POST /api/v1/trials/{id}/finalize 与 /terminate 竞争终态；GET /api/v1/trials/{id}/credential 查询凭据。稳定错误码包括BROKEN_PRESSURE_PATH、CALIBRATION_STALE、STEP_OUT_OF_ORDER、SAMPLE_OUT_OF_ORDER、RATE_EXCEEDED、LEAK_LIMIT_EXCEEDED、LEASE_EXPIRED、ROUND_STALE、IDEMPOTENCY_CONFLICT和FINAL_STATE_CONFLICT。
7. 浏览器入口 / 提供构型编辑、试验进度、租约状态、采样证据、复验轮次、人员复核与凭据验证页面，通过上述API完成真实读写；依赖锁文件及固定构建脚本确保可复现。

## 失败边界

1. 构型冻结、原子开试、阶梯完成、异常传播、新轮次创建和准入签发分别构成独立事务边界；失败时不得遗留部分绑定、租约、阶段、证据或凭据。
2. HTTP层仅完成解析、操作者标识检查和稳定错误映射；领域错误明细按轮次、舱段、安装位、样点时刻排序后返回。
3. 并发写入使用聚合版本条件更新和数据库唯一约束裁决；数据库忙仅按固定次数重试，超过上限返回STORE_BUSY。
4. 外部设备适配器不能直接推进试验状态。拒绝、断连、超时、格式错误和响应矛盾仅写入设备调用记录，经过领域验证的成功响应才能转为证据。
5. 重启时从已提交事件核对并恢复物化状态，仅关闭逻辑时刻已经到期的活跃租约标记；历史租约、旧轮次和异常证据不得删除。
6. 终态通过单条条件更新抢占；失败竞争者返回已提交结果或FINAL_STATE_CONFLICT，任何路径都不得生成第二份凭据。

## 验收标准

1. 构型冻结可稳定识别闭合承压结构、压力入口、密封边界、传感器校准和压力阶梯；摘要在重启前后完全一致。
2. 固定整数样例精确得到补偿压力、升降压速率、压力回落、泄漏率和阀门延迟；除零及int64溢出使事务整体失败。
3. 两个并发启动请求竞争同一泵组时仅一个成功；失败请求不留下绑定或其他租约，幂等重放返回原结果。
4. 合法阶段和压力阶梯形成连续前缀；乱序样点、越级推进、过期租约、错误轮次及超限窗口均不能推进状态。
5. 脚本化设备超时、校准过期、格式错误和矛盾响应只生成确定重试记录，不生成合格压力或阀位证据。
6. 四类异常生成唯一且规范排序的复验范围；新轮次隔离旧证据，并稳定拒绝旧轮次迟到输入。
7. 仅当全部阶梯、泄压、检查、复验清零和双人资质条件满足时生成唯一凭据；并发终态操作只有一个胜者。
8. 关闭并重启服务后，构型、试验阶段、事件、租约历史、证据、轮次和凭据一致；真实前端可经API完成主流程，amd64与arm64镜像均可构建。

## 确定性测试场景

1. configuration_test.go：有效双舱段构型冻结成功；孤立舱段和缺失压力入口被拒绝；重复安装位失败后数据库无快照。
2. pressure_math_test.go：固定样点精确断言补偿压力、速率、压力回落和泄漏率；半开窗口边界不重复计数；除零与乘加溢出被拒绝。
3. startup_lease_test.go：同步屏障驱动两个请求竞争同一泵组，仅一个整体成功；冲突事务不保留部件绑定；同操作号异内容返回幂等冲突。
4. trial_state_test.go：合法阶段和阶梯连续推进；跳过充液排气失败；租约恰好到期时样点被拒绝；乱序样点不进入证据链。
5. device_evidence_test.go：超时、校准过期、格式错误和阀位矛盾按固定逻辑时刻产生递增重试；达到上限仍无合格证据；稳定窗口满足后生成唯一阶梯证据。
6. retest_round_test.go：超压传播至所属舱段，渗漏传播至密封边界，共享管路异常覆盖关联安装位；并发重复报告只生成一个复验集合；旧轮次迟到样点返回ROUND_STALE。
7. finalization_recovery_test.go：同一人重复复核、资质过期和未清复验均阻止准入；同步并发准入、待复验与终止仅一个提交；重开SQLite后凭据和证据根一致。
8. api_frontend_test.go：API错误码与明细排序固定；浏览器入口由Go服务提供；页面通过真实API读取试验并提交样点与复核。
9. 公开测试至少分布于上述8个文件，包含不少于20个独立测试，统一使用固定构型、注入逻辑时钟、脚本设备、同步屏障和临时SQLite，不依赖墙钟、外部网络、随机调度或执行顺序。

## 组件追踪关系

1. 试验构型目录落实构型冻结、承压结构和整数参数规则，对应验收1、2，由configuration_test.go和pressure_math_test.go覆盖。
2. 耐压试验聚合落实阶段连续前缀、轮次事件和重启恢复，对应验收4、8，由trial_state_test.go和finalization_recovery_test.go覆盖。
3. 部件绑定与资源租约落实唯一安装位、原子启动、租约有效期和幂等行为，对应验收3、4，由startup_lease_test.go覆盖。
4. 采样与证据引擎落实样点排序、压力计算、稳定窗口和设备重试，对应验收2、5，由pressure_math_test.go与device_evidence_test.go覆盖。
5. 异常复验与准入仲裁落实传播范围、轮次隔离、独立复核、终态竞争和唯一凭据，对应验收6、7，由retest_round_test.go和finalization_recovery_test.go覆盖。
6. HTTP服务与操作前端落实版本化接口、稳定错误和真实页面交互，对应验收8，由api_frontend_test.go覆盖；Go包建议划分为configuration、trial、evidence、qualification、store和httpapi。

## 独特性

项目围绕深海采样器承压舱在航次前的分级耐压证明展开，以密封边界传播、温度补偿压力窗口、受控泄压、复装轮次隔离和证据根凭据为核心，使机械构型、传感器证据与最终准入形成可恢复且可确定验证的单一闭环。
